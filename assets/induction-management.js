(function(){'use strict';let profile,sessions=[];const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function form(s={}){const d=document.createElement('div');d.className='modal-backdrop';d.innerHTML=`<form class="modal"><h2>${s.id?'Editar':'Nueva'} inducción</h2><div class="form-grid"><div class="field"><label class="label">Nombre</label><input class="input" name="title" value="${esc(s.title||'Inducción de primer ingreso')}" required></div><div class="field"><label class="label">Proyecto (opcional)</label><input class="input" name="project_name" value="${esc(s.project_name)}"></div><div class="field"><label class="label">Fecha</label><input class="input" name="session_date" type="date" value="${esc(s.session_date)}" required></div><div class="field"><label class="label">Hora</label><input class="input" name="start_time" type="time" value="${esc((s.start_time||'').slice(0,5))}" required></div><div class="field"><label class="label">Enlace de reunión o portal</label><input class="input" name="meeting_url" type="url" value="${esc(s.meeting_url)}" required></div><div class="field"><label class="label">Cupo</label><input class="input" name="capacity" type="number" min="1" value="${esc(s.capacity||30)}" required></div><div class="field"><label class="label">Estado</label><select class="select" name="status">${[['scheduled','Programada'],['registration_open','Registro abierto'],['in_progress','En curso'],['closed','Cerrada'],['cancelled','Cancelada']].map(x=>`<option value="${x[0]}" ${s.status===x[0]?'selected':''}>${x[1]}</option>`).join('')}</select></div></div><div class="field"><label class="label">Notas internas</label><textarea class="input" name="notes">${esc(s.notes)}</textarea></div><div class="actions"><button class="btn primary">Guardar inducción</button><button type="button" class="btn secondary" data-close>Cerrar</button></div></form>`;document.body.append(d);d.querySelector('[data-close]').onclick=()=>d.remove();d.querySelector('form').onsubmit=async e=>{e.preventDefault();const v=Object.fromEntries(new FormData(e.target));v.capacity=Number(v.capacity);v.tenant_id=profile.tenant_id;v.created_by=s.created_by||profile.id;const q=s.id?OnboardAuth.client.from('of_sessions').update(v).eq('id',s.id):OnboardAuth.client.from('of_sessions').insert(v);const {error}=await q;if(error)alert(error.message);else{d.remove();load()}}}async function load(){profile=profile||await OnboardAuth.getProfile();const {data,error}=await OnboardAuth.client.from('of_sessions').select('*,participants:of_session_candidates(count)').order('session_date',{ascending:false});if(error){console.error(error);return}sessions=data||[];render()}function render(){const root=document.getElementById('induction');if(!root)return;root.innerHTML=`<div class="grid grid-4"><article class="card metric"><strong>${sessions.filter(s=>['scheduled','registration_open'].includes(s.status)).length}</strong><span>Próximas inducciones</span></article><article class="card metric"><strong>${sessions.reduce((n,s)=>n+(s.participants?.[0]?.count||0),0)}</strong><span>Personas asignadas</span></article><article class="card metric"><strong>${sessions.filter(s=>s.status==='in_progress').length}</strong><span>En curso</span></article><article class="card metric"><strong>${sessions.filter(s=>s.status==='closed').length}</strong><span>Cerradas</span></article></div><section class="card section"><div class="management-head"><div><h2>Calendario de inducciones</h2><p>Cree la sesión antes de aprobar candidatos.</p></div><button class="btn primary" data-new-session>Nueva inducción</button></div><div class="table-wrap"><table><thead><tr><th>Fecha</th><th>Hora</th><th>Nombre</th><th>Proyecto</th><th>Asignados</th><th>Estado</th><th>Acciones</th></tr></thead><tbody>${sessions.map(s=>`<tr><td>${esc(s.session_date)}</td><td>${esc(s.start_time)}</td><td>${esc(s.title)}</td><td>${esc(s.project_name||'Todos')}</td><td>${s.participants?.[0]?.count||0} / ${s.capacity||'—'}</td><td>${esc(s.status)}</td><td><button class="btn secondary" data-edit-session="${s.id}">Editar</button></td></tr>`).join('')||'<tr><td colspan="7">No hay inducciones creadas.</td></tr>'}</tbody></table></div></section>`}document.addEventListener('click',e=>{if(e.target.dataset.newSession!==undefined)form();if(e.target.dataset.editSession)form(sessions.find(s=>s.id===e.target.dataset.editSession))});document.addEventListener('DOMContentLoaded',load)})();
